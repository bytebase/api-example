// This file handles incoming Jira issue webhooks for both creation and updates
import { generateToken, fetchData, createIssueWorkflow, updateJiraIssue } from '../utils';

// ... (rest of the file remains the same)